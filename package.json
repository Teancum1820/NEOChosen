{
  "name": "neochosen-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static NEO Chosen event website for Cloudflare Pages.",
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "dev": "npm run build && wrangler pages dev dist",
    "deploy": "npm run build && wrangler pages deploy dist --project-name neochosen --branch main"
  },
  "devDependencies": {
    "wrangler": "4.123.0"
  }
}
